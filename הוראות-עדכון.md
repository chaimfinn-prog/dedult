# 🏆 הוראות עדכון — ניחושי המונדיאל

יש שני חלקים: **(א) הרצת SQL ב‑Supabase** (חד‑פעמי), ו**(ב) גרירת קובץ הבילד לנטליפיי**.
מומלץ בסדר הזה.

---

## חלק א — הרצת SQL ב‑Supabase (פעם אחת)

היכנס ל‑Supabase ← הפרויקט ← תפריט שמאלי **SQL Editor** ← לחצן **New query**.
הדבק כל קובץ בנפרד והרץ (Run). הקבצים נמצאים בתיקיית `supabase/` בפרויקט.

הרץ לפי הסדר:

1. **security-hardening.sql**
   - מפעיל את כפתורי האדמין (בלי זה הם נחסמים), נועל ניחושים אחרי שריקה,
     שומר חותמת זמן בשרת, ומתחיל לתעד שינויים (לוג ביקורת).
   - בדיקה: הרץ `select public.is_admin();` כשאתה מחובר — לא חובה שיחזיר true ב‑SQL Editor.
     המבחן האמיתי הוא באתר עצמו.

2. **score-snapshots.sql**
   - יוצר את טבלת ההיסטוריה לגרף "מירוץ המקומות".

3. **cron.sql**
   - לפני ההרצה החלף בקובץ: `<PROJECT_REF>` (מזהה הפרויקט) ו‑`<ANON_KEY>`.
   - מפעיל עדכון תוצאות אוטומטי עם **שמירת שבת**: אין משיכת תוצאות
     מ‑19:00 יום שישי עד מוצ"ש, ועדכון יחיד ב‑20:15 במוצ"ש.

4. **check-odds.sql** (בדיקה בלבד)
   - הרץ כדי לראות אם יש משחקים שהיחסים שלהם התבלבלו. מי שמסומן
     "⚠️ התיקו פייבוריט" צריך תיקון (אפשר דרך האדמין ← "תיקון יחסי 1X2 למשחק").

---

## חלק ב — גרירת הבילד לנטליפיי

1. פתח את הקובץ `dedult-netlify.zip` ופרק אותו (Unzip).
2. היכנס לנטליפיי ← האתר שלך ← לשונית **Deploys**.
3. **גרור את תוכן התיקייה שפורקה** (הקבצים שבפנים) לאזור הגרירה
   ("Drag and drop your site output folder here").
4. תוך כ‑20 שניות הגרסה החדשה באוויר. הכתובת לא משתנה — אין מה לעדכן בהגדרות.

> אם נטליפיי עדיין חוסם פריסות (חריגת קרדיטים) — חכה לאיפוס החודשי,
> או עבור ל‑Cloudflare Pages (יש לי הוראות נפרדות).

---

## חלק ג — בדיקות מהירות אחרי העדכון

- היכנס לאתר מחובר עם הגוגל שלך ← **ניהול** ← נסה לעדכן תוצאת משחק. אמור לעבוד.
- בעמוד **משחקים** — האתר נפתח על המשחק הבא; משחקים שנגמרו ב"📁 ארכיון משחקים".
- פתח משחק ← "👥 ניחושי החברים" ← נסה **"תמונה 🖼️"** ו‑**"וואטסאפ 💬"**.
- **סטטיסטיקה** ← טאב **"📈 מירוץ"** (יתמלא תוך כמה שעות ככל שנכנסים לטבלת המובילים).

---

## מה חדש ואיפה

| מה | איפה |
|---|---|
| ייצוא משחק כתמונה + שיתוף לוואטסאפ | עמוד משחקים ← פתיחת "ניחושי החברים" |
| ארכיון משחקים + פתיחה על המשחק הבא | עמוד משחקים |
| גרף מירוץ מקומות (מונפש) | סטטיסטיקה ← טאב "מירוץ" |
| מי לא מילא ניחושים | ניהול |
| שינויי ניחושים מעניינים (רגע אחרון / ביטל מנצח) | ניהול |
| תיקון יחסי 1X2 ידני למשחק | ניהול |
| שמירת שבת בעדכונים האוטומטיים | אוטומטי (cron.sql) |
